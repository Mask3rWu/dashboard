# Flight Analyzer 使用说明

Flight Analyzer 是用于飞行数据导入、管理与分析的软件。当前说明文档暂包含导入、数据管理、数据分析三部分。

## 1. 导入

### 功能说明

导入功能用于把外部飞行数据文件导入软件数据库，并建立“机型 - 飞机 - 飞行架次”的管理层级。导入时软件会自动扫描文件夹，识别数据类型、飞行场次、飞机序号和飞行日期，并在导入前提供预览。

导入后，软件会保存解析后的数据，同时转存本次导入关联的原始文件，便于后续追溯。

### 数据目录要求

导入路径必须是一个文件夹，不能直接选择单个文件。软件会递归扫描该文件夹下的 `.txt` 数据文件。

目录中必须存在一个以 8 位日期开头的日期目录，日期格式为 `YYYYMMDD`。推荐目录结构如下：

```text
20250323_test_flight/
  Aircraft001/
    xxx_120001.txt
    xxx_120002.txt
  Aircraft002/
    xxx_130001.txt
    xxx_130002.txt
```

目录规则说明：

- 日期目录名称必须以 `YYYYMMDD` 开头，例如 `20250323`、`20250323_test_flight`。
- 飞机序号建议放在日期目录的下一层，例如 `20250323_test_flight/Aircraft001/`。
- 数据文件可以继续放在飞机目录下的子目录中，软件会递归扫描。
- 软件会从日期目录提取默认飞行日期，并从日期目录下一层目录提取飞机序号。
- 如果目录中没有检测到日期目录，扫描会失败，需要先整理目录结构。
- 如果日期目录下没有飞机序号子目录，软件可能无法自动分配飞机，需要在导入界面手动选择或创建飞机。

### 使用流程

1. 进入“导入数据”页面。
2. 在路径输入框中填写飞行数据文件夹路径，或点击“浏览”选择文件夹。
3. 点击“扫描”。
4. 查看扫描结果，确认目录日期、飞机序号、识别到的数据类型和场次数量。
5. 确认或调整机型：
  - 如果匹配到已有机型，可直接使用推荐机型。
  - 如果发现新格式，需要输入机型名称，选择要保留的数据类型，然后创建机型。
6. 对每个待导入场次确认飞机、飞行日期和记录信息。
7. 点击对应场次的“导入”按钮。
8. 导入完成后，可在“已导入飞行”列表中查看结果。





### 导入注意事项

- 同一飞机、同一飞行日期、同一场次编号已导入时，软件会阻止重复导入。
- 如果不同飞机存在相同日期和时间的飞行，界面会提示冲突；确认确实属于不同飞机后仍可继续导入。
- 原始数据类文件可能分析价值较低，新建机型时默认不一定勾选，可按实际需要手动选择。
- 导入前请确认飞行日期正确。软件会根据目录日期自动填充，但用户可以在导入前修改。
- 记录信息包括地点、天气、设备载荷、燃油量、起飞重量、海拔高度、风速和备注，可在导入时填写，也可在后续数据管理中编辑。
- 如需自动预填记录信息，可在任意该架次 `.txt` 数据文件同级目录放置 `FlightRecord_<场次号>.xml`，例如 `FlightRecord_114430.xml`。扫描时如找到该文件，会自动填充对应场次的记录单，导入前仍可手动修改。
- 导入外场同步包时，需要先预览 `.fapkg` 包内容，再确认机型处理、飞机映射和重复架次策略。

### 飞行记录单 XML 格式

推荐格式如下，数值字段可留空；字段名也可以直接使用数据库字段名，例如 `record_location`、`record_weather`。

```xml
<?xml version="1.0" encoding="UTF-8"?>
<FlightRecord>
  <BatchName>第一批</BatchName>
  <TotalDurationMin>75</TotalDurationMin>
  <Location>试飞场A区</Location>
  <Weather>晴</Weather>
  <Payload>2.5</Payload>
  <FuelAmount>18.0</FuelAmount>
  <TakeoffWeight>52.3</TakeoffWeight>
  <Altitude>1200</Altitude>
  <WindSpeed>3.2</WindSpeed>
  <Note>自动预填示例</Note>
</FlightRecord>
```

字段对应关系：

- `TotalDurationMin` / `record_total_duration_min`：总时长，单位分钟。
- `Location` / `record_location`：地点。
- `Weather` / `record_weather`：天气。
- `Payload` / `record_payload`：设备载荷，按表单单位填写。
- `FuelAmount` / `record_fuel_amount`：燃油量，单位 kg。
- `TakeoffWeight` / `record_takeoff_weight`：起飞重量，单位 kg。
- `Altitude` / `record_altitude`：海拔高度，单位 m。
- `WindSpeed` / `record_wind_speed`：风速，单位 m/s。
- `Note` / `record_note`：备注。

---

## 2. 数据管理

### 功能说明

数据管理用于维护已导入数据的组织结构、基础信息和列定义。主要管理对象包括机型、飞机、飞行架次、原始文件和数据列。

### 主要功能

- 查看全局统计信息，包括总飞机数、总架次和总航时。
- 按机型查看飞机列表和对应飞行架次。
- 添加、重命名或删除飞机。
- 重命名或删除飞行架次。
- 编辑飞行记录信息，包括地点、天气、载荷、燃油、起飞重量、海拔、风速和备注。
- 查看某个飞行架次关联的原始文件清单，并打开原始文件目录。
- 查看和编辑列定义，包括数据组名称、字段显示名称和单位。
- 导入或导出机型配置。
- 按飞机代号、时间范围、地点、天气、载荷等条件筛选数据。

### 使用流程

1. 进入“数据管理”页面。
2. 在左侧机型列表中选择目标机型。
3. 在飞机列表中展开目标飞机，查看该飞机下的飞行架次。
4. 根据需要执行以下操作：
  - 点击“添加飞机”新增飞机代号。
  - 点击飞机或架次旁的“重命名”修改名称。
  - 点击“编辑记录”维护飞行记录字段。
  - 点击“原始文件”查看本次导入保存的原始文件。
  - 点击“分析”进入该架次的数据分析页面。
5. 如需调整字段显示，使用右侧“列定义”区域编辑数据组名称、字段名称和单位。

### 数据管理注意事项

- 删除机型、飞机或飞行架次会影响相关数据，请确认后再执行。
- 部分删除操作受当前运行环境、登录状态或权限控制影响，界面不可用时表示当前用户无权执行。
- 列定义影响分析页面中的字段名称和单位显示，修改前请确认含义准确。
- 原始文件清单用于追溯导入来源，不建议手动移动或删除软件转存目录中的文件。
- 飞行记录字段与解析数据不同，属于人工维护的补充信息，可用于筛选、统计和后续整理。

---

## 3. 飞行分析

### 功能说明

数据分析用于查看单个飞行架次的时序曲线、筛选数据、计算相关性、检测异常，以及对多个飞行架次进行同指标对比。

### 单飞行分析

单飞行分析主要用于查看一个架次内部的多列数据变化。

主要功能包括：

- 按机型、飞机和飞行架次选择分析对象。
- 按数据组选择需要显示的字段。
- 对字段设置缩放系数，便于不同量纲数据放在同一图表中观察。
- 保存和加载常用字段选择预设。
- 使用筛选条件过滤数据。
- 查看飞行时长等基础统计信息。
- 计算所选字段的 Pearson 相关系数矩阵。
- 选择单个字段进行异常检测，并在图表中查看异常点。

使用流程：

1. 从数据管理页面点击某个架次的“分析”，或在分析页面选择目标架次。
2. 在左侧数据列区域勾选需要分析的字段。
3. 如不同字段数值范围差异较大，可设置缩放系数。
4. 在曲线图中查看数据变化，并使用图表缩放功能查看局部细节。
5. 如需复用当前字段组合，可保存为预设。
6. 切换到相关性或异常检测视图，执行对应分析。

### 多飞行对比

多飞行对比用于比较多个飞行架次在同一指标上的差异。

使用流程：

1. 进入“多飞行对比”页面。
2. 选择机型和飞机。
3. 在架次列表中至少选择两个飞行架次。
4. 在指标下拉框中选择一个要对比的数据列。
5. 点击“对比”，查看多架次曲线。
6. 可使用搜索框筛选架次，或使用图表缩放查看细节。

### 数据分析注意事项

- 分析结果依赖导入时识别的数据类型和列定义；如果字段名称或单位不准确，请先在数据管理中修正列定义。
- 多飞行对比一次只能选择一个指标，适合观察不同架次在同一参数上的变化差异。
- 相关性分析需要先选择多个字段，字段过多时计算和阅读成本都会增加。
- 异常检测结果用于辅助定位可疑数据点，不能单独作为最终结论。
- 如果图表为空，请先确认该架次已成功导入对应数据类型，并确认左侧已勾选字段。
