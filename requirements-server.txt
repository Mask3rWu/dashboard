# Dependencies for the standalone MySQL collaboration server.
# Keep these out of requirements.txt so the desktop PyInstaller build stays small.
fastapi>=0.100.0
uvicorn>=0.30.0
sqlalchemy>=2.0
pymysql>=1.1
python-multipart>=0.0.9
